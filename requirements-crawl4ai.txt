# Optional final fallback for unusually complex, permitted JavaScript pages.
# This is deliberately separate because Crawl4AI brings a sizeable browser
# runtime; it is never needed for normal static competitor articles.
-r requirements-crawler.txt
crawl4ai==0.9.2
